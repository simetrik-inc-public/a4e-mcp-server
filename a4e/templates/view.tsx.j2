"use client";
import React from "react";

interface {{ view_name }}Props {
{% for prop_name, prop_info in props.items() %}
  {{ prop_name }}{% if prop_info.type == "string" %}?: string{% elif prop_info.type == "number" %}?: number{% elif prop_info.type == "boolean" %}?: boolean{% elif prop_info.type == "array" %}?: any[]{% elif prop_info.type == "object" %}?: Record<string, any>{% else %}?: any{% endif %};
{% endfor %}
}

export default function {{ view_name }}View(props: {{ view_name }}Props) {
  return (
    <div 
      className="relative flex flex-col items-center justify-center w-full h-full min-h-screen p-8"
      style={{ '{{' }}
        background: "linear-gradient(135deg, #f8fafc 0%, #f1f5f9 50%, #e2e8f0 100%)",
      {{ '}}' }}
    >
      {/* Subtle dot pattern overlay */}
      <div 
        className="absolute inset-0 opacity-[0.4]"
        style={{ '{{' }}
          backgroundImage: "radial-gradient(circle, #cbd5e1 1px, transparent 1px)",
          backgroundSize: "24px 24px",
        {{ '}}' }}
      />
      
      {/* Content */}
      <div className="relative z-10 text-center max-w-lg">
        {/* Icon */}
        <div className="mb-8">
          <div className="inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-white/80 backdrop-blur-sm shadow-lg shadow-slate-200/50 border border-slate-200/60">
            <svg className="w-10 h-10 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={{ '{' }}1.5{{ '}' }} d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6zM16 13a1 1 0 011-1h2a1 1 0 011 1v6a1 1 0 01-1 1h-2a1 1 0 01-1-1v-6z" />
            </svg>
          </div>
        </div>

        {/* Title */}
        <h1 className="text-3xl font-medium text-slate-700 tracking-tight">
          {{ view_name }}
        </h1>
        <p className="text-slate-400 mt-3 text-lg">
          {{ description }}
        </p>

        {/* Props Preview */}
{% if props %}
        <div className="mt-10 bg-white/70 backdrop-blur-sm rounded-2xl shadow-sm border border-slate-200/60 p-5 text-left">
          <p className="text-[11px] font-semibold text-slate-400 uppercase tracking-wider mb-3">
            Props received
          </p>
          <pre className="text-sm text-slate-600 overflow-auto font-mono">
            {{ '{' }}JSON.stringify(props, null, 2){{ '}' }}
          </pre>
        </div>
{% endif %}

        {/* Footer hint */}
        <p className="text-xs text-slate-400 mt-8">
          <code className="bg-white/60 px-2 py-1 rounded-md border border-slate-200/60 font-mono">
            views/{{ view_id }}/view.tsx
          </code>
        </p>
      </div>
    </div>
  );
}
