# {{ skill_name }} Skill

## Purpose

{{ description }}

## When to Use

This skill is triggered by phrases like:
{% for trigger in intent_triggers %}
- "{{ trigger }}"
{% endfor %}

## Instructions for View Renderer

{% if internal_tools %}
### 1. Call Internal Tools

{% for tool in internal_tools %}
```python
{{ tool }}_result = {{ tool }}({
    # Add required parameters here
})
```
{% endfor %}

### 2. Prepare View Parameters
{% else %}
### 1. Prepare View Parameters
{% endif %}

{% if view_props %}
Extract data and build the view params object:

```python
view_params = {
{% for prop_name, prop_info in view_props.items() %}
    "{{ prop_name }}": ...,  # {{ prop_info.get('type', 'any') }}{% if prop_info.get('description') %} - {{ prop_info.get('description') }}{% endif %}

{% endfor %}
}
```

### View Props Reference

| Property | Type | Description |
|----------|------|-------------|
{% for prop_name, prop_info in view_props.items() %}
| `{{ prop_name }}` | {{ prop_info.get('type', 'any') }} | {{ prop_info.get('description', '-') }} |
{% endfor %}
{% else %}
```python
view_params = {
    # Define view parameters based on tool results
}
```
{% endif %}

## Expected Output

```python
{% if internal_tools %}
# Step 1: Call tools to get data
{% for tool in internal_tools %}
{{ tool }}_data = {{ tool }}({})
{% endfor %}

# Step 2: Process and prepare view params
{% endif %}
view_params = {
{% for prop_name, prop_info in view_props.items() %}
{% set prop_type = prop_info.get('type', 'string') if prop_info is mapping else prop_info %}
{% if prop_type == 'string' %}
    "{{ prop_name }}": "{{ prop_info.get('default', 'Sample ' + prop_name) if prop_info is mapping else 'Sample ' + prop_name }}",
{% elif prop_type == 'number' %}
    "{{ prop_name }}": {{ prop_info.get('default', 0) if prop_info is mapping else 0 }},
{% elif prop_type == 'boolean' %}
    "{{ prop_name }}": {{ prop_info.get('default', 'False') if prop_info is mapping else 'False' }},
{% elif prop_type == 'array' %}
    "{{ prop_name }}": [],
{% elif prop_type == 'object' %}
    "{{ prop_name }}": {},
{% else %}
    "{{ prop_name }}": None,
{% endif %}
{% endfor %}
}
```

## Parameter Extraction Rules

Map user intents to parameters:

| User Says | Extracted Parameters |
|-----------|---------------------|
{% for trigger in intent_triggers[:3] %}
| "{{ trigger }}" | `{}` |
{% endfor %}

## Notes

- **Output view:** `{{ output_view }}`
{% if requires_auth %}- **Authentication:** Required{% else %}- **Authentication:** Not required{% endif %}
{% if internal_tools %}- **Dependencies:** {{ internal_tools | join(', ') }}{% endif %}

